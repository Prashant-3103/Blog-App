let express,dotenv;_56b‍.w("express",[["default",["express"],function(v){express=v}]]);_56b‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);

dotenv.config();
const app = express();
app.use(express.json());
app.get('/',(req,res)=>{
    res.send("server is running")
});

app.listen(5000, ()=>lo)
